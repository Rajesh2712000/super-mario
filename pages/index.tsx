import type { NextPage } from "next";
import Head from "next/head";
const Home: NextPage = () => {
  return (
    <Head>
      <div>
        <div>Chess</div>
      </div>
    </Head>
  );
};

export default Home;
